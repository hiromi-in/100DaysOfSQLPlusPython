### Day2: Show unique product categories.
  
SQL: Used distinct function. 
Python: value_counts() function. Also added a histogram to see the distribution of each categories.

Comment: It is still a simple command so both SQL and Python work with no effort.