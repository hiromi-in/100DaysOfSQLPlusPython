### Day5: Get total revenue (price × quantity).

Today's target was aggregation with some filters.
SQL: Used sum function with filters.
Python: groupby().sum() function. 
Comment: I didn't notice the issue with the data type at first. Although it looks like correct information in the result, it could not be data types which can be used with other functions. So it is always a good idea to check data types.